import { NotificationService } from '../services/NotificationService';

export class NotificationController {
    constructor(private service: NotificationService) { }

    // Futuras rotas para gerenciar notificações podem ser adicionadas aqui
    // public async getUserNotifications(req: Request, res: Response): Promise<Response> {
    //     // Lógica para listar as notificações de um usuário
    // }
}